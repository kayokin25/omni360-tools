# В репозитории держим LF, чтобы правки на разных машинах не давали шумных диффов.
* text=auto eol=lf

# Собранная панель — один длинный файл, построчный дифф по нему бесполезен.
dist/** -diff
source/** -diff
